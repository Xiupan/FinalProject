<h1>Final Project Home</h1>
<%= form_tag root_path, :method => :get, :class => 'empire-form' do %>
  <div class="empire-dropdown">
    <%= collection_select :name, :id, Empire.all, :id, :name %>
    <button type="submit" class="btn">Search</button>
  </div>
<% end %>
